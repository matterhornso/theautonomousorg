node_modules/
.next/
.gstack/
.env*
data/*.db

# clerk configuration (can include secrets)
/.clerk/

# OS / editor cruft
.DS_Store

# Private marketing data + outbound docs (PUBLIC repo — never commit)
marketing-ops/
the-autonomous-cold-sequence.md

# SQLite WAL/SHM files (transient, per dev-server boot)
data/*.db-shm
data/*.db-wal

# Build artifacts
next-env.d.ts
tsconfig.tsbuildinfo

# Playwright test artifacts
test-results/
playwright-report/

# Skill bundles (installed from skills-lock.json)
.claude/skills/
.agents/skills/
